# Disable all preinstall, postinstall, and lifecycle scripts by default.
# This stops malicious packages from running arbitrary code on your machine upon download.
ignore-scripts=true

# Enforce strict peer dependency resolution to prevent unexpected version hijacking.
strict-peer-dependencies=true

# Prevent auto-installing missing peer dependencies, forcing you to audit them manually.
auto-install-peers=false